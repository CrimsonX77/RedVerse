# Environment Configuration Template
# Copy this file to .env and fill in your values

# ─── Python Environment ────────────────────────────────────────
# Update this to match your Python installation
PYTHON_EXECUTABLE=/path/to/your/python
# Example: /home/username/.pyenv/versions/3.10.16/bin/python

# ─── Project Directory ────────────────────────────────────────
# Absolute path to your RedVerse installation
PROJECT_DIR=/path/to/your/Redverse
# Example: /home/username/Projects/Redverse

# ─── Google Cloud Speech (Optional - for Scribe) ──────────────
# Path to your Google Cloud credentials JSON file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/credentials.json

# ─── Ollama Configuration ──────────────────────────────────────
# Ollama API endpoint (default: http://localhost:11434)
OLLAMA_HOST=http://localhost:11434

# ─── OpenAI API (Optional) ─────────────────────────────────────
# If using OpenAI instead of Ollama
# OPENAI_API_KEY=your_api_key_here

# ─── Soul System Directories ───────────────────────────────────
SOULS_DIR=eros_souls
SAVE_DIR=eros_outputs
MEMORY_DIR=memory

# ─── Stripe Payment Configuration ──────────────────────────────
# Your Stripe secret key (get from https://dashboard.stripe.com/apikeys)
# Use test key (sk_test_...) for testing, live key (sk_live_...) for production
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Stripe webhook secret (optional - for webhook endpoint)
# Get from https://dashboard.stripe.com/webhooks
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# ─── Server Configuration ──────────────────────────────────────
# Port for the payment server (default: 5000)
PORT=5000

# Flask environment (development or production)
FLASK_ENV=development

# ─── JWT AUTHENTICATION (Phase 2) ───────────────────────────────
# Generate a strong random secret key:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production

# JWT token expiry in hours
JWT_EXPIRY_HOURS=24

# ─── GOOGLE OAUTH CONFIGURATION ─────────────────────────────────
# From Google Cloud Console > APIs & Services > Credentials
GOOGLE_CLIENT_ID=23326348224-utv5ng3jq4u21ort7of2hgaed6hkql3u.apps.googleusercontent.com

# CRITICAL: Server-side secret, NEVER expose in frontend
# Store this securely and never commit to git
GOOGLE_CLIENT_SECRET=your-google-client-secret-key

# Your GitHub Pages URL (for authorized JavaScript origins)
REDVERSE_BASE_URL=https://your-github-username.github.io

# ─── MEMORY SYSTEM CONFIGURATION ────────────────────────────────
# Directory for per-user memory JSONL files
MEMORY_THREADS_DIR=memory/threads

# Enable memory encryption at rest (Phase 3)
MEMORY_ENCRYPTION_ENABLED=false

# Enable cross-site memory summary (requires Tier 4+)
CROSS_SITE_ENABLED=true

# Enable memory sharing between users
MEMORY_SHARING_ENABLED=true

# ─── SECURITY & RATE LIMITING ──────────────────────────────────
# Max API calls per user per minute
RATE_LIMIT_PER_MINUTE=100

# ─── ADMIN CONFIGURATION ───────────────────────────────────────
# Email addresses of admin users (comma-separated)
# These users get access to Crimson Control Hall
ADMIN_EMAILS=admin@example.com,staff@example.com

